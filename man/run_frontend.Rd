% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_frontend.R
\name{run_frontend}
\alias{run_frontend}
\title{Run the sparse-to-dense conversion inside a Docker container}
\usage{
run_frontend(
  local_dir,
  file,
  features_file,
  barcodes_file,
  outputfile,
  image = "matrixtransform:latest"
)
}
\arguments{
\item{local_dir}{Local directory containing \code{matrix.mtx},
\code{features.tsv} and \code{barcodes.tsv}; mounted as \code{/scratch}.}

\item{file}{Name of the sparse matrix file, relative to \code{local_dir}.}

\item{features_file}{Name of the features file, relative to \code{local_dir}.}

\item{barcodes_file}{Name of the barcodes file, relative to \code{local_dir}.}

\item{outputfile}{Name of the output CSV file, written into \code{local_dir}.}

\item{image}{Name of the Docker image to run.}
}
\value{
Invisibly, the path of the generated output file.
}
\description{
Front-end that launches a Docker container, mounting a local directory as a
volume (\code{/scratch}), and runs \code{mattransform} inside it. Because the
output is written to \code{/scratch}, it appears directly in the mounted local
directory.
}
\examples{
\dontrun{
run_frontend("/data/sparse_matrix", "matrix.mtx", "features.tsv",
             "barcodes.tsv", "dense.csv")
}

}
