% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mattransform.R
\name{mattransform}
\alias{mattransform}
\title{Convert a 10x sparse matrix to a dense matrix}
\usage{
mattransform(file, features_file, barcodes_file, outputfile)
}
\arguments{
\item{file}{Path to the sparse matrix file (\code{matrix.mtx}).}

\item{features_file}{Path to \code{features.tsv}; its first column labels
the rows (genes).}

\item{barcodes_file}{Path to \code{barcodes.tsv}; it labels the columns
(cells).}

\item{outputfile}{Name of the output CSV file for the dense matrix.}
}
\value{
A named numeric vector with the number of rows and columns of the
  dense matrix.
}
\description{
Reads a 10x Genomics sparse matrix (\code{matrix.mtx}) together with its
barcodes and features, assigns the barcodes to the columns and the features
to the rows, converts the matrix to a dense representation and writes it to
a CSV file.
}
\examples{
\dontrun{
mattransform("matrix.mtx", "features.tsv", "barcodes.tsv", "dense.csv")
}

}
